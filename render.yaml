services:
  - type: web
    name: ai-support-chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.address 0.0.0.0 --server.fileWatcherType none
    envVars:
      - key: GROQ_API_KEY
        sync: false